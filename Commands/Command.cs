namespace HangmanSix
{
    /// <summary>
    /// Keeps all possible commands given from player
    /// </summary>
    public enum Command
    {
        Help,
        Top,
        Exit,
        Restart,
        Used
    }
}
